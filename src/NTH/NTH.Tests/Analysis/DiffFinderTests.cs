using NUnit.Framework;

namespace NTH.Tests.Analysis
{
    [TestFixture]
    public class DiffFinderTests
    {
        /*
        [Test]
        public void GetCommonPrefixLengthTest()
        {
            var p = new PrivateType(typeof(DiffFinder<string>));
            var res = Convert.ToInt32(p.InvokeStatic("GetCommonPrefixLength", new[] { typeof(char[]), typeof(char[]) }, new [] { "abc".ToCharArray(), "xyz".ToCharArray() }));
            Assert.AreEqual(res, 0);

            res = Convert.ToInt32(p.InvokeStatic("GetCommonPrefixLength", "1234abcdef", "1234xyz"));
            Assert.AreEqual(res, 4);

            res = Convert.ToInt32(p.InvokeStatic("GetCommonPrefixLength", "1234", "1234xyz"));
            Assert.AreEqual(res, 4);
        }
        */
    }
}
